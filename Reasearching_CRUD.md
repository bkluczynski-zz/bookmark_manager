```

The CRUD stands for 'Create, Read, Update and Delete', what describes the methodology that shall be applied when interacting with databases.

The SQL concept is Structured Query Language which permits the user to interact with a database.

SQL examples are DELETE WHERE, SELECT * FROM, UPDATE SET or CREATE DATABASE


```
